{
  "name": "learn-tls",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "babel index.js -o build.js -s inline",
    "start": "npm run build && node build"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "asn1": "^0.2.2",
    "babel": "^5.1.13",
    "source-map-support": "^0.2.10"
  }
}
